export interface AboutProfile {
  name: string
  intro: string
  description: string
  skills: string[]
  now: string[]
}
export interface AboutProfile {
  name: string
  intro: string
  description: string
  skills: string[]
  now: string[]
}
